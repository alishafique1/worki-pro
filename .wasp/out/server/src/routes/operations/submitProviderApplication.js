import { createAction } from '../../middleware/operations.js'
import submitProviderApplication from '../../actions/submitProviderApplication.js'

export default createAction(submitProviderApplication)
