import { createAction } from '../../middleware/operations.js'
import resubmitProviderApplication from '../../actions/resubmitProviderApplication.js'

export default createAction(resubmitProviderApplication)
