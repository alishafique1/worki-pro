import { createAction } from '../../middleware/operations.js'
import createBillingSetupSession from '../../actions/createBillingSetupSession.js'

export default createAction(createBillingSetupSession)
