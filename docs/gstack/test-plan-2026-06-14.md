# Test Plan — worki-pro Launch Increment — 2026-06-14

## Automated

| Gate | Command | Result (2026-06-14) |
|------|---------|---------------------|
| Type check | `npx tsc --noEmit` | ✅ exit 0 |
| Full build | `wasp build` | ✅ compiled + built (SDK regenerated, no errors) |
| E2E (Playwright, 70 tests) | `wasp test` (needs `wasp start db` + dev server) | ⏳ pre-deploy gate — baseline 66/70 (4 known strict-mode locator collisions, not app bugs) |

## Manual Smoke (run on staging/dev before deploy)

1. **Consumer onboarding interests:** role=Consumer → profile → step 3 shows category picker → select 2 → Complete Setup → success screen with 3 cards → verify `ConsumerInterest` rows created and listings personalize.
2. **Skip path:** same flow, "Skip for now" → lands on `/get-quotes`, no interest rows, no error.
3. **OTP origin check:** POST `/api/auth/request-otp` with a foreign `Origin` header → 403; normal SPA login → works.
4. **OTP attempt cap:** 5 wrong codes → 429; valid code still rejected after cap until new code requested.
5. **Password length:** direct `verifyOtp` POST with 4-char password → 400.
6. **Provider resubmit:** rejected provider opens `/provider/profile` → "Resubmit" → status → PENDING, admin email queued.
7. **404 recovery:** hit a bad URL logged-out → Go Back Home → landing; logged-in consumer → `/dashboard`; Browse services → `/services`; Contact support → `/contact`.
8. **Skeletons:** throttle network on `/listings`, `/dashboard`, `/my-requests` → skeletons render, no layout shift.

## Regression Watch

- Onboarding sessionStorage persistence still works across refresh.
- Provider 4-step flow unchanged (only consumer step 3 + nav visibility changed).
- Signup-bonus still issued exactly once under retry (dedup guard).

## Critical-Path Smoke (post-deploy, prod)

Lead submit → provider claim ($5, idempotent) → appointment book → review submit → reward pending. Plus `GET /api/health` → 200.
